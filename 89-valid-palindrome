public Boolean isPalindrome(String str){
    //code here
    Pattern nonAlphanumeric = Pattern.compile('[^a-zA-Z0-9]');
    Matcher matcher = nonAlphanumeric.matcher(str);
    String forword = matcher.replaceAll('');
    System.debug(forword);
    return true;
}