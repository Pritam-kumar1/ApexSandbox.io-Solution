public void plusOne(List<Integer> numbers) {
    // code here
    for(Integer i = numbers.lenght()-1;i>=0;i--){
        numbers[i]++;
        if(numbers[i]<=9)
            break;
        else
            numbers[i] = 0;
    }
    numbers.add(0,1);
}