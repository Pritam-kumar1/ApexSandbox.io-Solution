public void plusOne(List<Integer> numbers) {
    // code here
    String s = String.join(numbers,',');
    Integer myInt = Integer.valueOf(s)+1;
    String a = String.valueOf(myInt);
    for(Integer i =0;i<a.length();i++){
        numbers[i] = (Integer.valueOf(a[i]));
    }
}